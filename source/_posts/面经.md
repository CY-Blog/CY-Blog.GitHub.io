---
title: 面经
date: 2023-06-13 16:19:59
tags:
 - 技术
 - 工作 
position: both
categories: 
 - 技术
cover: http://p2.qhimg.com/bdm/1280_791_0/t01e6c5bf87456d6c82.jpg
---
# 牛客
>宇通
>1. socket工作方式，接口？
1.tcp 2.udp 
    -  接口 客户端：connect send recv 服务端 bind listen accept send recv
2. linux解压和压缩命令
    - tar
3. usb扫描逻辑
    - 检测：通过总线检测设备
 识别：根据对应的设备id加载对应驱动 最后分配设备号分配内存 使用
4. spi时序
    - 四根线 
    SCLK（时钟信号）：由主设备产生，用于同步数据传输。
    MOSI（主设备输出，从设备输入）：主设备将数据发送到从设备。
    MISO（主设备输入，从设备输出）：从设备将数据发送到主设备。
    SS（片选信号）：由主设备产生，用于选择与之通信的从设备。
5. 数组和链表区别
    - 存储方式：数组是一段连续的内存空间，用于存储相同类型的元素；链表是由节点组成，每个节点包含数据和指向下一个节点的指针。
    内存分配：数组在创建时需要分配固定大小的内存空间，而链表可以动态分配节点，根据需要进行扩展或缩小。
    插入和删除操作：数组在中间插入或删除元素时需要移动其他元素，而链表只需要修改指针即可。
    访问效率：数组通过下标可以直接访问元素，访问效率较高；链表需要从头节点开始遍历，访问效率较低。
    内存占用：数组的大小固定，可能造成内存浪费；链表根据实际需要动态分配内存，更加灵活。
6. 一级指针指向的值改变之后，对应的二级指针是否会发生改变
    - 不会 因为二级指针存储地址
7. lcd屏幕工作原理
    - 在驱动里分为几个部分 水平和垂直的刷新在不同的lcd有不同的参数 这些事 电子枪打到屏幕上的时间 具体屏幕可以分为很多 但是是有三原色组成红黄蓝这些数据被放到显存
8. spi传输时命令和数据如何判断
    - 有命令和数据两条线
9. spi工作模式有哪些
    - 主模式 从模式 主从机传输

> 科大讯飞
>1. IIC接口地址位有几位，只有这一种模式吗,理论上能接多少外设，实际上能接多少，是什么影响的
    - 七位 理论上是128 但是实际上会有地址冲突 信号干扰这些因素影响 有高速标准模式
2. 写过IIC驱动吗，为什么要这么去实现，简述一下
    - 裸机：
    裸机就是记住有几个寄存器 从地址寄存器 控制寄存器 数据寄存器
    时序图：
    写：开始信号 发送设备地址后面跟着读写位 发送应答信号 重新开始 发送寄存器地址 数据
    读：复杂一点 就是开始的从地址是写 然后最后的步骤是读
    - 总线：
    就是先修改设备树 根据硬件图 然后直接有一个结构体 在结构体上进行修改 
3. 聊聊C语言基础知识吧，说说什么是堆和栈吧，在mcu中程序运行的空间是什么样的
    - 栈存储着局部变量 函数参数和上下文信息 栈的大小是固定的 而堆的分配是动态的
4. static和const的区别，常用在什么地方，空间上有什么区别吗
    - static关键字用于修改变量和函数的作用域。在全局变量中，static关键字使变量的作用域限定在当前文件内，即变量对其他文件是不可见的。在局部变量中，static关键字使变量的生命周期延长到整个程序执行期间，而不是只在其定义的块中有效。
    const关键字用于声明常量，即数值在程序执行期间不可修改。常量的值在初始化后不能再被改变，这样可以增加代码的可读性和安全性。const关键字通常用于修饰变量、函数参数和函数返回值，以确保它们不被意外修改。
5. static修饰的在其他文件能使用吗。
    -  不能 不可见
6. 说一下局部变量与全局变量的区别。在MCU上空间上有什么不同吗？
    - 位置不同 一个在栈里 一个在静态区 生命周期不同 一个是当前函数结束 一个是程序开始到结束



>诺瓦星云

 


