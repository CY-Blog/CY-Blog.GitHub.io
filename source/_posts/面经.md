---
title: 面经
date: 2023-06-13 16:19:59
tags:
 - 技术
 - 工作 
position: both
categories: 
 - 技术
cover: http://p2.qhimg.com/bdm/1280_791_0/t01e6c5bf87456d6c82.jpg
---
# 基础部分
## 驱动中操作物理绝对地址为什么要先ioremap?
示例代码：
``` c
	IMX6U_CCM_CCGR1 = ioremap(regdata[0], regdata[1]);
	SW_MUX_GPIO1_IO03 = ioremap(regdata[2], regdata[3]);
  	SW_PAD_GPIO1_IO03 = ioremap(regdata[4], regdata[5]);
	GPIO1_DR = ioremap(regdata[6], regdata[7]);
	GPIO1_GDIR = ioremap(regdata[8], regdata[9]);
```
因为linux内核操作的都是虚拟地址 所以要进行一部映射才能访问到对应地址 但是在后买你的基于设备树的驱动上 pinctrl已经为我们完成了电器属性和引脚复用 gpio已经为我们完成了设备的具体引脚 做好了这些事情 我们不需要自己在驱动文件里进行影射了 我们只需要在对应的设备里使用对应的api`gpio_set_value` 类似这种
## 设备驱动模型三个重要成员是？platform总线的匹配规则是？在具体应用上要不要先注册驱动再注册设备？有先后顺序没？
总线，驱动，设备
1. 名称匹配
2. 设备树匹配

设备可以在设备树里注册，也可以通过代码注册设备，匹配成功会去调用驱动程序里的probe函数（probe函数在这个platform_driver结构体中注册）。
## insmod 一个驱动模块，会执行模块中的哪个函数？rmmod呢？这两个函数在设计上要注意哪些？遇到过卸载驱动出现异常没？是什么问题引起的？
会执行模块中的`module_init()`函数 而rmmod 会执行 `module_init()`
## copy_to_user()和copy_from_user()主要用于实现什么功能?一般用于file_operations结构的哪些函数里面?
yinwei1